<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%=foundUser.username%>'s Pantry</title>
  </head>
  <body>
    <%- include('../partials/_navbar.ejs') %>

    <h1>Welcome to <%=foundUser.username%>'s Pantry</h1>

    <%if(foundUser.pantry.length) { %>
    <ul>
      <% foundUser.pantry.forEach((food) => { %>
      <li><%=food.name%></li>
      <% }) %>
    </ul>

    <% } else { %>
    <p>There are no items in your pantry!</p>
    <% } %>
  </body>
</html>
